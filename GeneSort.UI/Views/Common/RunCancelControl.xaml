<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GeneSort.UI.Views.Common"
             xmlns:Converters="clr-namespace:GeneSort.UI.Converters" 
             x:Name="userControl" 
             x:Class="GeneSort.UI.Views.Common.RunCancelControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <Converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid ForceCursor="True">

        <Button Background="Orange"
                    x:Name="RunButton" 
                    Visibility="{Binding IsRunning, Converter={StaticResource InverseBoolToVisibilityConverter}, ElementName=userControl, Mode=OneWay}" 
                    Command="{Binding RunCommand, ElementName=userControl}"
                    Content="Run"/>
                
        <Button Background="Aqua" x:Name="CancelButton" 
                Visibility="{Binding IsRunning, Converter={StaticResource BoolToVisibilityConverter}, ElementName=userControl, Mode=OneWay}" 
                Command="{Binding CancelCommand, ElementName=userControl}"
                Content="Cancel"/>

    </Grid>
</UserControl>

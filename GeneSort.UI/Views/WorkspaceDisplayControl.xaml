<UserControl x:Class="GeneSort.UI.Views.WorkspaceDisplayControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GeneSort.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Workspace Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left">
            <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="Bold" Margin="0,0,10,0"/>
            <TextBlock Text="-" FontSize="18" Margin="0,0,10,0"/>
            <TextBlock Text="{Binding Description}" FontSize="14" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- DataGrid for Run Parameters (Dynamic Columns) -->
        <DataGrid x:Name="ParametersDataGrid" 
                  Grid.Row="2" 
                  ItemsSource="{Binding RunParametersData}" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  GridLinesVisibility="All"
                  HeadersVisibility="Column"
                  Margin="0,10,0,0"/>
    </Grid>
</UserControl>

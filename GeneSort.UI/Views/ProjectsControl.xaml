
<UserControl x:Class="GeneSort.UI.Views.ProjectsControl"             
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GeneSort.UI.Views" 
             xmlns:converters="clr-namespace:GeneSort.UI.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="Project Configuration"
                   FontSize="24"
                   FontWeight="Bold"
                   Foreground="#007ACC"
                   HorizontalAlignment="Left"
                   Margin="0,0,0,20"/>

        <!-- Project Folder Selection -->
        <StackPanel Grid.Row="1" Orientation="Vertical">
            <Label Content="Project Folder:" Style="{StaticResource ModernLabel}"/>
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding ProjectFolder, Mode=OneWay}"
                         Style="{StaticResource ModernTextBox}"
                         Width="500"
                         IsReadOnly="True"/>
                <Button Content="Browse..."
                        Command="{Binding BrowseProjectFolderCommand}"
                        Style="{StaticResource ModernButton}"
                        MinWidth="80"/>
            </StackPanel>

            <!-- Experiments Section -->
            <Border Background="White"
                    CornerRadius="8"
                    BorderBrush="#E0E0E0"
                    BorderThickness="1"
                    Margin="0,20,0,0">
                <Grid Margin="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Description -->
                    <TextBlock Grid.Row="0"
                               Text="The subfolders in the selected project represent individual experiments."
                               FontSize="14"
                               Foreground="#666666"
                               Margin="0,0,0,16"
                               TextWrapping="Wrap"/>

                    <!-- Experiments List -->
                    <ListBox Grid.Row="1"
                             ItemsSource="{Binding Experiments}"
                             SelectedItem="{Binding SelectedExperiment, Mode=TwoWay}"
                             Style="{StaticResource ModernListBox}"
                             Margin="0,0,0,16"/>

                    <!-- Open Experiment Button -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Open Experiment"
                                Command="{Binding DataContext.OpenExperimentCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding SelectedExperiment}"
                                Style="{StaticResource ModernButton}"
                                IsEnabled="{Binding SelectedExperiment, Converter={x:Static converters:BoolConverters.NotNullToBool}}"
                                MinHeight="30"
                                MinWidth="120"/>
                    </StackPanel>
                </Grid>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>